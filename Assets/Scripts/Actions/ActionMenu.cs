using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ActionMenu : MonoBehaviour
{
    private List<int> actionIds = new();

    //for selected actor
    //get list of available actions (as IDs)
    //populate menu with corresponding labels, UI events -> actions
    //??? create UI event for selecting the correct data
        //UI manager/component which you put into different selection states
    //on confirming send corresponding selection payload to action


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
